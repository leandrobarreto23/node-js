// buffer -> representacao de um espaco na memoria do computador, salvar e ler na memoria de uma forma muito perfomatica

//transforma em código binario (hexadecimal)

const buf = Buffer.from("hello")

console.log(buf.toJSON())